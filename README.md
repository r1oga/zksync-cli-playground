# zkSync CLI playground

Play around with zksync on Rinkeby.  
Interactive CLI to:

- deposit from ETH (rinkeby) network to Zksync (rinkeby) network.
- register (register public key) account on ZkSync (rinkeby).

## Getting started: `yarn cli`